# KitchenFlow-v1 dependencies
# Pinned for reproducibility — update carefully

numpy>=1.26,<2.0
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.0,<3.0

# Optional: Gradio UI (comment out to slim image)
gradio>=4.0,<5.0

# Optional: training libraries (not required for env or server)
# gymnasium>=0.29,<1.0
# stable-baselines3>=2.3,<3.0
# torch>=2.0,<3.0
